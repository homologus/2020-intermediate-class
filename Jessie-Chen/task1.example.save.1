from Bio import SeqIO
reads = list(SeqIO.parse("/share/SARS/SARS-2020.fasta","fasta"))

for i in range(0,ranslatedSeq1 = frameworke1.seq.translate()
numLongSeq = 0
newSequences1 = translatedSeq.split("*")
for i in reads: 
	frame1 = i.seq[0::]
	frame2 = i.seq[1::]
	frame3 = i.seq[2::]
for i in range(0,len(newSequences)):
        untested = newSequences[i]
        if len(untested) >= 100:
                numLongSeq = numLongSeq + 1

print("There are",numLongSeq,"Long Sequences")


 


