def findOriginal(str):
	first = []
	last = []
	word = []
	a_order = "".join(sorted(str))
	#print(a_order)
	index = 0

	for i in a_order:
		first.append(i)
		
	for x in str:
                last.append(x)
	
	length = len(str)

	word.insert(0, last[0])

	indexes = [0]
	while(len(word)!= length -1):
		x = word[0]
		i = first.index(x)
		indexes.append(i)
		if(
		word.insert(
	
	print(word)
			

findOriginal("DVAAN$E")

